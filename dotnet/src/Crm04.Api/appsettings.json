{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "// ConnectionStrings": "Company Oracle (mill4db) - the same schema Crm04.Feeder writes. WARNING: this file is committed, so the password travels with the code. Before pushing to a shared or public repository, remove it and set the ConnectionStrings__Crm04 environment variable on the server instead - an environment variable overrides this value.",
  "ConnectionStrings": {
    "Crm04": "User Id=CRM04;Password=CRM04@123;Data Source=132.147.244.24:1521/mill4db;"
  },

  "Source": {
    "// Kind": "Oracle - the plant feed, read from what Crm04.Feeder writes. REQUIRED: there is no default, and an unset or unrecognised value refuses to start. 'Replay' (simulator output) is refused outside Development; see appsettings.Development.json.",
    "Kind": "Oracle",
    "Oracle": {
      "// StartAtLatest": "Begin at the newest frame rather than replaying history a 10 Hz UI could never catch up from.",
      "StartAtLatest": true,
      "// CatchUpThreshold": "If more frames than this are pending, publish only the newest. The API holds one MachineState, not a queue.",
      "CatchUpThreshold": 5
    }
  },

  "Cors": {
    "Origins": [ "http://JLD2SRV19913:5240", "http://localhost:5240", "https://localhost:7120" ]
  }
}
